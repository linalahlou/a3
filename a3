#include <iostream>
#include <fstream>
#include <string>
#include <sstream> 
#include <vector>
#include <stdlib.h>


using std::vector;
using namespace std;


int main() {
	string fileName;
	string line;
	ifstream fin;
	//ofstream fout;
	char chunkLength[4];
	char chunkType[4];
	
	char sig[8];
	char crc[4];
	
	// opening greeting
	cout << endl;
	cout << "Image Processing Software" << endl;
	cout << endl;
	cout << "Specify the name of a PNG file that you would like to process." << endl;
	cout << ">";
	cin >> fileName;
	
	// reading file 
	//if (fileName=="brainbow") {
		fin.open(fileName, ios_base::in | ios_base::binary);
		//if (fin) {
			cout << "Loading "<< fileName << endl<< endl;
			fin.read(sig,8);
			while (!fin.eof()) {
				fin.read(chunkLength,4);
				fin.read(chunkType,4);
				unsigned long l = ((unsigned char)chunkLength[0]<<24) + ((unsigned char)chunkLength[1]<<16) + ((unsigned char)chunkLength[2]<<8) + ((unsigned char)chunkLength[3]);
				char* chunkData = new char[l];
				fin.read(chunkData,l);
				fin.read(crc,4);
				unsigned long q = ((unsigned char)crc[0]<<24) + ((unsigned char)crc[1]<<16) + ((unsigned char)crc[2]<<8) + ((unsigned char)crc[3]);

				
				cout << l << endl;

				//if (chunkType == IHDR)
					
			}
			
		fin.close();	
		//}
	//} else cout << "Unable to open file"; 
	
	return 0;
}

//if (chunkType[0] == '73' && ...)  then iHDR
	
